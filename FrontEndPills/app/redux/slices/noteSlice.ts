import { createSlice } from "@reduxjs/toolkit";

const initialState = {}

const noteSlice = createSlice({
    name:'noteSlice',
    initialState,
    reducers:{}
}) 

export default noteSlice.reducer
